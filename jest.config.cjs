/* como en este ejercicio no se está usando fetch API entonces no se instaló "npm install --save-dev whatwg-fetch" ni se creó un archivo "jest.setup.js" */

module.exports = {
  testEnvironment: "jest-environment-jsdom",
  // setupFiles: ['./jest.setup.js']
};
